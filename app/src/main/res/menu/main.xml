<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:tools="http://schemas.android.com/tools"
        xmlns:deliverone="http://schemas.android.com/apk/res-auto"
      tools:context=".MainActivity">
    <item android:id="@+id/action_qr_code"
        android:title="QR Code"
        deliverone:showAsAction="ifRoom"
        android:icon="@drawable/qr_code"/>
    <!--<item android:id="@+id/action_maps"
        android:title="@string/action_maps"
        deliverone:showAsAction="ifRoom"
        android:icon="@drawable/map_marker"/>-->
    <item   android:id="@+id/action_shopping_cart"
            android:icon="@drawable/shopping_cart"
            deliverone:showAsAction="ifRoom"
        android:title="shopping_cart" />
    <item android:id="@+id/action_settings"
          android:title="@string/action_settings"
          android:orderInCategory="100"
          android:showAsAction="never"/>

</menu>
